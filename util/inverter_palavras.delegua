funcao inverter_palavras(texto) {
    var separar_palavras = texto.dividir(' ');
    var frase_invertida = "";

    para var i = separar_palavras.tamanho() - 1; i >= 0; i = i - 1 {
    frase_invertida = frase_invertida + separar_palavras[i] + " ";
    } 

    retorna frase_invertida;
}